<div class="all_deals">
  <h2>My Coupons</h2>

  <table class="table">
    <tr>
      <th>Business</th>
      <th>Deal</th>
      <th>Expires On</th>
      <th>Redeemed</th>
    </tr>

  <% @coupons.each do |coupon| %>
    <tr>
      <td><%= coupon.deal.advertiser.business_name%></td>
      <td><%= link_to coupon.deal.coupon_text, coupon.deal %></td>
      <td><%= coupon.deal.end_date.strftime('%D') %></td>
      <td><%= print_redeemed(coupon.redeemed) %></td>
    </tr>
  <% end %>

  </table>
</div>