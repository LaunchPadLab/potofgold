$('.backstretch').remove();
$.backstretch("<%= j(@image.image_url) %>");
$('.deal_container').slideDown('slow').delay(1000);
$('#deal_image_id').val('<%= @image.id %>');
$('#background_image_form').slideUp();